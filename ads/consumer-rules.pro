# The GMA Next-Gen SDK and Firebase ship their own consumer ProGuard rules.
# Keep this module's public API surface used reflectively by nothing special,
# so no additional rules are required for consumers.
